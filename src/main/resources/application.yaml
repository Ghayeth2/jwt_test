security:
  jwt:
    secret-key: theSecretKeyshoulBeLongAndCompLex4PrOdUcTiOnmanners
spring:
  datasource:
    url: jdbc:h2:file:./data/jwt_security
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
      path: /h2-console